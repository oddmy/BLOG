server:
  port: 1100

spring:
  application:
    name: server-gateway
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9000/eureka
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
